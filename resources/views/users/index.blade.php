@extends('layouts.app')
@section('page-title','User management')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><p class="tiny mb-0">Create and manage secure system accounts and roles.</p><a class="btn btn-brand btn-sm" href="{{route('users.create')}}"><i class="bi bi-person-plus me-1"></i>Add user</a></div>
<form class="toolbar mb-3" method="get"><div class="input-group" style="max-width:320px"><span class="input-group-text bg-white"><i class="bi bi-search"></i></span><input class="form-control" name="search" value="{{request('search')}}" placeholder="Search by name or email"></div><button class="btn btn-light btn-sm">Search</button></form>
<div class="panel p-0 overflow-hidden"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th class="px-3">User</th><th>Role</th><th>Status</th><th>Created</th><th class="text-end px-3">Actions</th></tr></thead><tbody>@forelse($users as $user)<tr><td class="px-3"><strong>{{$user->name}}</strong><small class="d-block text-muted">{{$user->email}}</small></td><td><span class="badge text-bg-light border text-capitalize">{{str_replace('_',' ',$user->role)}}</span></td><td>@if($user->is_active)<span class="text-success small"><i class="bi bi-check-circle"></i> Active</span>@else<span class="text-muted small"><i class="bi bi-pause-circle"></i> Inactive</span>@endif</td><td class="small text-muted">{{$user->created_at?->format('d M Y')}}</td><td class="text-end px-3"><a class="btn btn-sm btn-outline-secondary" href="{{route('users.edit',$user)}}"><i class="bi bi-pencil"></i></a>@if(!$user->is(auth()->user()))<form class="d-inline" method="post" action="{{route('users.destroy',$user)}}" onsubmit="return confirm('Delete this user account?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form>@endif</td></tr>@empty<tr><td colspan="5" class="text-center p-5 text-muted">No user accounts found.</td></tr>@endforelse</tbody></table></div><div class="p-3 border-top">{{$users->links()}}</div></div>
@endsection
